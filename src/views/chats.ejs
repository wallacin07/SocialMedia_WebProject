<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="chatList">
        <% chats.forEach(chat => { %>
            <a href="/chats/<%= id %>/<%= chat.idChat %>" class="chatBox">
                <div>
                    <%= chat.idChat %>
                </div>
            </a>
        <% }) %>
    </div>
</body>
</html>